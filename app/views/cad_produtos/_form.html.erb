<%= form_with(model: cad_produto, local: true) do |form| %>
  <% if cad_produto.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cad_produto.errors.count, "error") %> prohibited this cad_produto from being saved:</h2>

      <ul>
      <% cad_produto.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :unidade_id %>
    <%= form.text_field :unidade_id %>
  </div>

  <div class="field">
    <%= form.label :produto_id %>
    <%= form.text_field :produto_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
